export const environment = {
  production: true,
  apiUrl: '/api',
  fileBaseUrl: '',
  // Voir environment.ts pour le détail — doit être la même valeur que côté dev et que
  // GOOGLE_CLIENT_ID côté backend.
  googleClientId: ''
};
